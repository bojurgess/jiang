pub mod median_cut {
    use crate::colour::Colour;

    pub fn quantize(pixels: &[Colour], k: usize) -> Vec<Colour> {
        todo!()
    }
}

pub mod octree {
    use crate::colour::Colour;

    pub fn quantize(pixels: &[Colour], k: usize) -> Vec<Colour> {
        todo!()
    }
}
